import { AttributedirectDirective } from './attributedirect.directive';

describe('AttributedirectDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributedirectDirective();
    expect(directive).toBeTruthy();
  });
});